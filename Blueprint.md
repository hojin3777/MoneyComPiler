개인화 마이데이터 앱 아키텍처 (The Big Picture)
아래 그림은 우리 앱의 전체 구조와 데이터 흐름을 시각적으로 표현한 것입니다.

각 구성 요소의 역할
Electron (전체 앱의 껍데기 및 관리자)

역할: 사용자가 실행할 .exe 설치 파일을 만듭니다. 앱의 창(Window)을 생성하고, 전체적인 생명주기(시작, 종료)를 관리합니다.
주요 임무:
앱이 시작될 때, 보이지 않는 곳에서 Python 백엔드 프로세스를 실행시킵니다.
사용자에게 보여줄 UI(웹 페이지)를 창 안에 로드합니다.
UI와 Python 백엔드 사이의 안전한 다리(통신 채널) 역할을 합니다.
앱이 종료될 때, 실행했던 Python 프로세스를 깔끔하게 종료시킵니다.
프론트엔드 (UI, 사용자의 눈과 손)

기술: HTML, CSS, JavaScript (React나 Vue 사용 추천)
역할: 사용자가 직접 보고 상호작용하는 모든 화면을 담당합니다. (대시보드, 차트, 달력, 버튼, 입력 폼 등)
주요 임무:
사용자 행동(버튼 클릭, 이미지 업로드 등)을 감지합니다.
필요한 작업(예: "이 이미지 OCR 해줘!")을 Electron을 통해 Python 백엔드에 요청합니다.
백엔드로부터 받은 데이터(JSON)를 이용해 화면을 동적으로 업데이트합니다. (예: 차트 다시 그리기, 목록에 새 항목 추가)
백엔드 (Brain, 실제 일꾼)

기술: Python (Flask 또는 FastAPI 프레임워크 사용)
역할: 눈에 보이지 않는 곳에서 모든 핵심 로직과 계산을 처리합니다. UI가 없습니다.
주요 임무:
프론트엔드의 요청을 받아, y_predict.ipynb에서 구현한 OCR 및 데이터 구조화 로직을 실행합니다.
**SQLite 데이터베이스에 데이터를 저장(INSERT), 조회(SELECT), 수정(UPDATE), 삭제(DELETE)**하는 모든 작업을 전담합니다.
대시보드에 필요한 통계 데이터를 DB에서 집계(GROUP BY, SUM 등)하여 계산합니다.
처리된 결과를 JSON 형태로 프론트엔드에 돌려줍니다.
데이터베이스 (Memory, 영구 저장소)

기술: SQLite
역할: 모든 거래 내역, 사용자 설정, 카테고리 등을 저장하는 앱의 '기억'입니다.
주요 임무:
사용자의 PC 내 안전한 폴더(예: 내 문서)에 my_data.db 같은 단일 파일로 존재합니다.
오직 Python 백엔드만이 이 파일에 접근하여 데이터를 읽고 씁니다. (보안상 매우 중요한 원칙)
대표적인 작업 흐름: "이미지 업로드로 내역 추가하기"
사용자: 앱 화면에서 '이미지 업로드' 버튼을 클릭하고 은행 앱 캡처 화면을 선택합니다.
프론트엔드(UI): 선택된 이미지 파일을 Electron을 통해 백엔드(Python)로 전달하며 "OCR 처리해줘"라고 요청합니다.
백엔드(Python): 이미지 파일을 받아 ML 모델을 실행하고, 거래 내역을 추출/정제합니다.
백엔드(Python): 정제된 거래 내역 데이터를 로컬 SQLite DB 파일에 저장(INSERT)합니다.
백엔드(Python): "성공적으로 처리했고, 추가된 데이터는 이것이야" 라는 응답을 JSON 형태로 프론트엔드에 보냅니다.
프론트엔드(UI): 응답을 받고, 화면의 거래 내역 목록과 대시보드 차트를 새로운 데이터로 업데이트하여 사용자에게 보여줍니다.




1. 좌측 패널은 화면 좌측 상단에 패널메뉴(☰) 버튼에 마우스를 호버하면 나타나고, 메뉴의 구성 요소는 Dashboard, Monthly, Transactions, Catergories로 구성. 패널 메뉴 하단에는 (?), (톱니바퀴 모양)의 도움말과 설정 버튼 우측으로 정렬.
2. 대시보드 메뉴
- 대시보드 메뉴 진입 시 기존 UI와 비슷하게 가운데 상단에 DASHBOARD 제목 표시
- 좌측엔 월별 지출 추이, 우측엔 월별 입출금 흐름을 표시할 것이지만 지금은 데이터가 없으므로 박스로 영역만 표시
3. 월력형 메뉴
- 상단엔 20XX년 N월 제목 표시하며 각 년도와 월은 누를 경우 드롭다운 목록으로 선택할 수 있게 동작
- 기존 UI와 동일하기 일월화...토 까지 요일 표시 후 월력형으로 사용 내역 표시. 지금은 데이터가 없으므로 달력만 표시
3. 거래내역 메뉴
- 상단 영역엔 엑셀과 유사하게 거래내역을 편집하고, 보조 입력 도구들을 넣을 영역을 정의. 변경내용 저장, 열 삭제, 굵게, 형광펜 등 강조 표시 버튼을 구현하고, 내역입력 폼 열기 버튼, 딥러닝 자동입력 열기 버튼을 일단 그려놓기
- 이후 아래 영역에는 날짜, 계좌, 유형, 대분류, 소분류, 금액, 거래처, 메모 순으로 열 제목을 스크롤하더라도 상단에 고정되어 있게 구현(엑셀의 첫 행 고정)
- 그 아래에는 엑셀처럼 값을 수동으로 변경하거나, 계좌, 유형, 대분류, 소분류 등은 미리 카테고리 메뉴에서 정의한 내역을 입력할 수 있게 드롭다운 목록 구현
- 금액 셀은 자동으로 세자리 콤마 형식을 적용하며, 날짜 열 좌측에는 빈 체크박스 버튼 열을 만들어 선택된 열들을 삭제하거나 이동할 수 있게 구현
4. 카테고리 메뉴
- 1행1열엔 계좌, 그 이후 대분류들이 표시되도록 셀 형태로 구현(상단에 계좌, 고정수입, 유동수입, 식비 .... 순으로 표시되도록)
- 계좌 행 제목 아래 작성한 이름들은 계좌DB에 저장
- 대분류 아래 작성된 항목들은 소분류로 저장.


### 대시보드 페이지
- 상단: 기간 필터 (연/월 선택)
- Phase 1: 
    - 좌측 영역:
        - 월간 상세 분석
            - 지출/수입 유형 비율: 차트 2개 표시(도넛차트는 총 금액을 비교하기 어려워 변경)
            - 전체 대분류별 지출 비율: 해당 월에 지출한 대분류의 지출액과 총지출 대비 비율을 테이블로 나열
            - 계좌별 잔고 현황: 각 계좌의 잔액 표시
    - 우측 영역:
        - 월별 지출/수입 추이: 사용자가 설정한 기간의 월별 총수입과 총지출을 누적 세로 막대형으로 표시, 기존과 달리 지출을 같이 표시하여 변화를 직관적으로 파악, 마우스 hover로 디테일한 내용 툴팁으로 표시
        
- Phase 2:
    - 좌측 영역:
        - 소비 습관 히트맵: 특정 요일에 어떤 소비(대분류 또는 소분류) 소비가 높아지는지 히트맵 제공
        - 카테고리 심층 분석: 특정 대분류 내 소분류들의 기간별 지출 추이 변화 표시
        - CustomContentTreemap을 활용한 대분류-소분류 모든 지출 비율 한눈에 보기
        - 예산 관리: 특정 카테고리들에 대한 월 단위 예산 관리
            1. database.py에 테이블 생성, dashboard_utils.py에 호출 함수 생성 후 app.py에 백엔드 API생성. 이때 년/월 호출 쿼리는 월간 상세 분석 or 대분류별 지출 비율과 동일하게 selectedYear과 selectedMonth 사용.
            2. '예산 관리' 카드 신설. 카드 컨텐츠 영역의 열 제목은 "대분류 선택-소분류 선택-사용금액-예산 progress bar-예산 설정" 순으로 배치.
                - 대분류/소분류 선택은 매핑 메뉴(mapping.tsx)의 룰베이스 매핑 select가 구현된 방식 및 레이아웃, 디자인을 그대로 옮길 것. 이때 주의할 것은 대분류에서 fixed 값 '고정수입, 유동수입, 이체분류' 3개의 대분류는 노출되지 않도록 할 것. FloatingSelectPopup을 사용하지 않는 이유는 이게 부모 요소 크기를 따라가서 카드 좌우폭이 좁아지면 팝업 셀렉트 요소가 다 구겨질 것으로 예상.
                - 사용금액은 선택된 해당 월의 대분류 or 소분류 사용 금액을 집계하여 표시.
                - 예산 progress bar는 월간 상세 분석 카드의 대분류별 지출 표에서 비율 열에 사용한 것과 동일한 렌더 방식 및 스타일을 사용할 것. 대신 색상은 highlight 3,4,5,6,1 순으로 사용하되 예산이 초과된 경우 highlight 2(빨간색)로 변하도록 설정.
                - 예산 설정은 기존 금액 표시 형식을 유지하되(우측정렬, 3자리 콤마 등), 다른 메뉴에서 적용된 UX(엔터또는 외부 클릭시 편집 저장, esc로 취소) 또한 마찬가지로 적용
            3. 카테고리 메뉴와 유사하게 (+) - 원안에 든 + 버튼을 행 가운데에 배치하여 새 예산 행을 추가할 수 있도록 하며 예산 설정 칸은 똑같이 hover 시 초록색 연필 아이콘과 빨간색 휴지통 아이콘을 배치하여 금액을 수정하거나 해당 예산 행을 삭제할 수 있도록 구성(삭제 클릭 시 confirmpopup을 띄우는 로직까지 동일하게). 카테고리 메뉴와 다른점은 0개 행이어도 상관이 없으며 일단 30개 항목까지 예산을 설정할 수 있도록 구성.
            4. 해당 카드 내에 수정사항(대분류/소분류/예산 설정/추가삭제) 발생 시 따로 저장버튼 없이도 settings 저장하듯이 바로바로 백엔드 API로 저장.
            5. 다른 카드들과 동일하게 행 갯수가 많아져서 카드 영역을 초과할 것 같은 경우 스크롤 요소로 바뀌며 카드 높이가 깨지지 않도록 css레이아웃 지정
    - 우측 영역:
        - 소비 TOP 10: 설정된 기간 내 가장 많이 지출한 소분류 TOP10 그래프 표시
            - 지출액/지출 횟수를 기준으로 좌우 세로 바 차트 형태
            - 툴팁에 거래처 및 금액을 모아 표시하여 어디에 많이 소비했는지 분석
            - 동일한 카테고리가 있는 경우 강조 표시
        - 이상 지출 탐지: 월별 지출액을 과거n월 평균 지출액과 비교하여 소비 이상치 탐지
        - 고정비 관리 대시보드: 고정지출의 목록 및 출금일자 모아보기
            1. 테이블 구성: 월별 예산 관리와 유사하게 테이블 리스트로 표시
            - 거래처명, 카테고리, 평균 출금일, 평균 지출액, 증감, 월별 지출 현황 5개 컬럼으로 표시(더 추가되면 좋겠다 싶은건 제안바람)
            - 백엔드에서 거래처명-카테고리 조합이 UNIQUE인 거래내역끼리 묶어서 프런트로 전달. 이때 카테고리 열에서는 대분류와 소분류를 묶어 "{대분류}-{소분류}"로 표시.
            - 평균 출금일, 평균 지출액: 받은 거래내역들을 기반으로 출금일과 지출액의 범위 표시. 예: 22일 ~ 24일, 30,000원 ~ 34,000원. 매달 똑같은 값만 지출되는 경우(넷플릭스처럼) 1일 / 14,000원 처럼 단일 값만 표시하도록 지정
            - 월별 지출 현황: 선택한 기간의 마지막 3개월을 기준으로 고정금액을 지출했는지의 여부 표시. "ㅇㅇㅇ"처럼 동그라미 아이콘 또는 div요소로 3개 표시하며, 거래내역이 있으면 accent-posivite, 없으면 accent-warning으로 표시
            - 증감: 제목 행에서는 표시하지 않고 "평균 지출액"과 병합하거나 "평균 지출액" 텍스트의 위치를 조절. 아이콘 하나 들어갈 정도의 얇은 열로 구성하여, 금액 바로 우측에 마지막 달의 지출액이 그 직전 달보다 오른 경우 accent-red색 삼각형 표시, 줄어든 경우 accent-blue 색의 역삼각형 표시. 없는 경우 공란.
            2. 마우스 hover 시 추가 정보 제공:
                - '증감'열과 '월별 지출 현황'의 인디케이터들은 마우스 hover시 작은 툴팁들 제공(전월 대비 증가/감소, YYYY년 M월 결제완료 등)
                - 거래처명 또는 카테고리 열 위에 마우스 hover시 디테일 툴팁 팝업 출력. 툴팁은 '대분류별 지출 비용'차트의 툴팁과 동일하게 헤더에는 거래처 이름 - 기간 내 총{count}(월 {averagecount})회 지출 표시. 구분선 아래에는 상세 거래내역 표시. 상세 거래내역은 YYYY-MM-DD - 금액으로 표시.
            3. 관련 백엔드 API신설: 해당 카드를 구현하며 필요한 백엔드 데이터 처리 함수와 API를 dashboard_utils.py와 app.py에 추가.
        - 자산 변화 추이: 전체 계좌의 합계가 월별로 어떻게 변하는지 표시
            1. 계좌의 월별 잔액 API 신설. 이때 기존 get_account_balances를 보면 현재 잔액은 계산이 가능한데, 묶은 영역 그래프로 표현하려면 월별 누적 데이터가 필요할 듯.
            2. 탭 구성: 기간 내 소비 TOP10 헤더 옆에 스위치 아이콘으로 두 기능을 넣은 것 처럼, 이쪽엔 양쪽 위는 둥근 모서리, 아래쪽은 구분선과 직각으로 만나는 탭 구성을 만들고, 두 탭 아이콘은 원형 차트와 영역 차트를 나타내는 아이콘으로 채울 것. 누른 아이콘에 따라서 카드에 표시할 내용을 달리할 것
            3. 도넛 차트(계좌별 잔액 비율)을 표시하고, 가운데에는 보유금액 총액을 나타낼 것. 첨부한 스크린샷처럼 가운데에는 총액이 상시 표시되고, 각 조각에 마우스 hover 시 디테일한 정보를 제공하는 방식이 좋을 듯. 필요시 해당 에제 코드 제공 요청할 것. 그 옆에는 계좌별 잔액 테이블을 표시하되, '월간 상세 분석' 카드의 '계좌 잔고' 테이블을 일단 그대로 옮겨올 것.
            4. 영역 차트 탭; 계좌별 누적 그래프를 표시하고, 첨부한 두번째 스크린샷처럼 Brush로 확대 및 스크롤이 가능하게 할 것. 이때 Brush의 label에 index 숫자가 아닌 YYYY-MM으로 표기할 수 있는지? 또한 그래프는 기본적으로 부모 요소의 슬라이더 range에 종속되지 않고 항상 전체 기간을 기준으로 데이터를 가져올 것. hover 시 해당 달의 말일을 기준으로 금액을 표시하고 전월 대비 얼만큼 오르고 내렸는지 툴팁 표시
            5. 우측 상단 dashboard-card-subtitle은 "전체 기간 기준"으로 표시


### 카테고리 페이지
- 카테고리 메뉴는 첨부한 두번째 이미지처럼 상단엔 계좌와 대분류를 나타내는 제목 열이 있고, 그 아래에는 해당 요소에 속하는 계좌나 소분류를 입력할 거야.
- Categories 옆에 저장 버튼을 만들어서 저장을 누르는 경우 수정된 내용이 DB에 저장되도록 구현하자.
- 기본값 기능을 구현하고 싶은데, 백엔드쪽 코드에 미리 기본값을 넣어둬야 하나? 계좌의 기본 요소는 계좌1, 나머지는 내가 미리 써둔 소분류를 따라서 기본값이 있으면 좋겠어
- 이체분류 오른쪽과 각 하위소요 하단엔 동그라미로 감싼 (+) 버튼을 놓고 새 요소를 추가할 수 있게 구성. 대분류는 최대 30개까지 제한을 두고, 대분류를 30개 채운 경우 더 이상 + 버튼이 안떴으면 좋겠어. 하위 요소도 마찬가지로 최대 20개까지.
- 가로로 스크롤하여 각 대분류 요소를 탐색할 수 있게 하단에 가로 스크롤바 구현
- 각각의 요소에 마우스를 호버하면 해당 요소 블럭은 흐려지며 연두색 동그라미에 연필 모양(수정)과 빨간색 휴지통 모양(삭제)이 보이고 수정과 삭제 기능을 구현.


### 거래내역 페이지
- 기존 '변경내용 저장' 버튼은 카테고리에서와 같이 Transactions 제목 우측으로 이동하며, 카테고리에서와 같이 아이콘만 표시하도록 하고 똑같이 초기화 버튼도 옆에 만들어 각각의 기능 구현
- 기존 '변경내용 저장' 자리에는 '열 삽입' 버튼 생성. 기본적으로 새 열 삽입은 맨 아래에 이루어지도록 하며, 체크박스가 선택된 열이 있는 경우 여러 줄을 선택하더라도 선택된 열 중 가장 마지막에 선택된 열 바로 아래에 새 열 삽입하도록 구현.
- 각 열 제목에는 엑셀의 '필터' 기능처럼 각 열에서 중복된 값을 제거하고 첨부된 이미지 처럼 체크박스 리스트를 띄워서, SQL AND 문으로 필요한 요소만 골라서 볼 수 있도록 필터 기능 구현
- 각 열은 옅은 색으로 각 셀이 구분될 수 있도록 구현
- 날짜 열의 내용 셀은 클릭할 시 SAP의 날짜 F4 기능 처럼 작은 달력이 작성되어있는 날짜 기준으로 띄워져 날짜를 작성하기 편리하도록 구성
- 계좌 열의 내용은 셀 클릭 시 첨부한 두번째 이미지처럼 드롭다운 목록으로 계좌 DB에 있는 계좌목록을 가나다순이 아닌 DB에 저장된 순으로 출력
- 유형 열은 [고정수입, 유동수입, 고정지출, 반고정지출, 유동지출, 이체] 6가지가 지금 제시된 순서대로 드롭다운 목록으로 표시되도록 구현
- 대분류, 소분류 열 또한 카테고리 메뉴에서 지정된 값이 가나다순이 아닌 지정된 순서대로 출력되도록 구성하며, 소분류는 선택한 대분류에 따라 출력되는 값이 달라지도록 구성(현재 엑셀에서는 INDIRECT 함수를 통해 대분류 값을 읽어서 구현)
- 금액 열 내용은 클릭 시 숫자를 편집할 수 있도록 구성하며, 기본적으로 입력된 값은 모두 음수로 처리하며 파란색으로 출력하고, +1,000 처럼 앞에 +가 붙은 경우에만 입금으로 처리하며 빨간색으로 값 표시
- 거래처, 메모는 선택 시 일반 텍스트 값 입력
- 저장 클릭 시 메모 열을 제외한 모든 셀은 올바른 값이 채워져 있어야 하며, 빈 값이 있을 시 경고

#### 자동입력기
- 이미지는 되도록 편하게 넣을 수 있도록 구현(구글 이미지와 유사하게)
    - 자동입력기를 누르면 팝업에 큰 파일 업로드 영역이 나오고, 해당 영역에 여러 이미지 파일을 드래그할 수 있도록 구현
    - 또는 업로드 영역을 클릭하는 경우 시스템 팝업을 통해 이미지 파일들을 선택하도록 구현
- 이미지 -> YOLO -> OCR+후처리 -> BERT 파이프라인을 통과하여 이미지로부터 거래내역 추출
- 추출 완료 후 거래내역에 삽입하기 전에 확인하는 팝업 생성(표 형태)
    - 기존 거래내역 UI의 축소판 팝업을 띄워서, 데이터 삽입 전 최종 수정이 가능하도록 구현(이미지가 여러개인 경우 한 표에 모두 들어가도록 정리)
    - 더치페이 정산 기능 구현: 특정 행을 잡고 드래그해서 다른 행 위에 포개면 ㄴ> 아이콘과 함께 해당 거래내역 밑으로 들어가서, 더치페이 대상 거래내역 행의 기존 금액에 취소선을 표시하고 정산된 금액 표시(가능하려나?)
- 데이터 삽입 버튼을 통해 최종 데이터 삽입


### 매핑 페이지
- 대부분의 딥러닝 모델 동작을 설정하는 곳
- 3개의 블럭으로 UI를 나눠서 구성, 좌측: 카테고리-딥러닝 출력 매핑, 우측 상단: OCR결과-다른 상호명으로 자동보정, 우측 하단: 상호명-카테고리 룰베이스 매핑
- 카테고리-딥러닝 출력 매핑
    - 세로 스크롤 형태로 대분류(소분류1, 소분류2, 소분류3...) 모두 세로 형태로 출력
    - 사용자 편의를 위해 기본 매핑 지정
    - 드래그&드롭으로 딥러닝 출력을 원하는 소분류 옆에 갖다두어 매핑 조정.
    - 소분류는 UUID 기준으로, 딥러닝 모델은 API의 출력과 관계없이 id:name 1:1 매핑이라 깨지지 않음
- OCR 결과-다른 상호명으로 자동보정
    - OCR결과에서 결제된 이름이 실제 상호명과 다른 경우, 사용자가 이를 수정할 텐데 이 불편함을 덜기 위해 구성
    - ex)자동입력기 메뉴에서 '(주)빅바이트컴퍼니'로 인식된 항목을 사용자가 거래내역 삽입 전 이를 '쉑쉑버거'로 바꾸면, 여기 메뉴에 '(주)빅바이트컴퍼니'->'쉑쉑버거' 자동 추가
    - 여기 메뉴에서 직접 추가/수정/삭제 가능
    - 두 데이터 모두 string으로 저장
- 상호명-카테고리 룰베이스 매핑
    - 특정 상호명을 다른 카테고리로 오인하여 사용자가 이를 직접 수정하는 경우 자동 추가(위의 메뉴와 유사)
    - ex) 'S.T.P.C.짱치킨' 을 딥러닝 모델이 'PC방'으로 인식하여 사용자가 이를 '식비-외식'으로 바꾸면, 여기 메뉴에 'S.T.P.C짱치킨'->'식비-외식' 자동 추가
    - 여기 메뉴에서 직접 추가/수정/삭제 가능
    - 여기 메뉴에 존재하는 상호명은 딥러닝 모델과 관계없이 바로 지정된 카테고리로 처리
    - 소분류는 UUID 기준으로 처리, 상호명은 string으로 저장



### DB 정의
#### 대분류 테이블
컬럼            타입        설명    
id              i           PK
name            string      대분류 이름
display_order   i           표시 순서

#### 소분류 테이블
컬럼                타입        설명
uuid                string      PK
name                string      소분류 이름
major_category_id   i           FK, 대분류 참조키
display_order       i           표시 순서

#### 계좌 테이블
컬럼            타입        설명
id              i           PK
name            string      계좌 이름
display_order   i           표시 순서

#### 거래내역
컬럼                타입        설명
id                  i           PK
transaction_date    string      거래날짜
account_id          i           FK, 계좌id참조
type                string      소비유형
minor_category_uuid string      FK, 소분류 uuid 참조
amount              i           금액
merchant            string      거래처
memo                string      메모




#### 임시 아이디어
2. 일일 예산 관리 (월력형 메뉴 확장) ⭐⭐⭐ (High Priority)
이유: 기존 엑셀 가계부의 유용한 기능, 일별 소비 관리에 효과적

구성:

월력형 메뉴 상단에 "일일 예산: ○○,○○○원" 설정 영역 추가
각 날짜 셀에:
해당 일 지출액 표시
일일 예산 대비 초과 시 빨간색 배경 + 경고 아이콘
남은 예산 표시 (예산 - 지출액)
월말 기준 "평균 일일 지출" 통계 표시
백엔드 API 추가:

3. 대분류별 월간 트렌드 비교 ⭐⭐ (Medium Priority)
이유: 카테고리별 심층 분석, Treemap 툴팁보다 독립 카드가 더 유용

구성:

상단: 대분류 선택 드롭다운 (FloatingSelectPopup 사용)
메인 차트: 선택된 대분류 내 소분류별 월간 지출 추이 (Stacked Area Chart)
각 소분류를 색상별로 구분
마우스 hover 시 특정 월의 소분류별 금액 툴팁 표시
하단 테이블: 전월 대비 증감률 순위
소분류 | 이번 달 | 지난 달 | 증감률 | 증감 아이콘
활용 시나리오: "식비가 늘었는데, 외식이 늘어난 건지 장보기가 늘어난 건지 확인"

5. 월별 수입/지출 밸런스 카드 ⭐⭐ (Medium Priority)
이유: 월별 추이에서 확인 가능하지만, 순저축률과 재정 건전성 지표 추가하면 차별화

구성:

메인 차트: 월별 수입 vs 지출 (Diverging Bar Chart)
수입: 위쪽(+), 지출: 아래쪽(-)
순저축 = 수입 - 지출을 중앙선 기준으로 표시
우측 지표:
순저축률: (수입 - 지출) / 수입 × 100
목표 달성률: 설정한 월간 저축 목표 대비 달성률
적자/흑자 개월 수: 최근 N개월 중 흑자/적자 개월 카운트
6. 이상 지출 탐지 (Anomaly Detection) ⭐ (Low Priority, Phase 3)
이유: 흥미롭지만 데이터가 충분히 쌓여야 유의미

구성:

과거 N개월(예: 6개월) 평균 지출액 계산
현재 월 지출이 평균 + 표준편차 × 1.5 초과 시 경고
어느 카테고리에서 이상 지출이 발생했는지 하이라이트
"이번 달 교통비가 평균보다 43% 증가했습니다" 알림