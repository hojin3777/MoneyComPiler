### TODOs
<details>
<summary>TODO</summary>

- TODOOOO: 카테고리 대분류 삭제할때 소분류와 달리 검증하지 않는 문제
- TODO: 윈도우 버전 torch 라이브러리 왜이렇게 큰지 보기
- TODO: 리셋 했을때 바로 저장까지 해버리지 않도록 수정
- TODO: apple developer 등록 무료?
- TODO: 예빈맥에서 딥러닝 기능 사용시 500 Internal server error 찍힘

좀 먼 일
- TODO: 딥러닝 모델 ONNX 마이그레이션 및 테스트
- TODO: GPU감지 UI 추가 및 설치 과정에서 CUDA 드라이버 설치 유도
- TODO: 자동 업데이트 기능 추가
- TODO: Github Releases 배포 설정
- TODO: 맥/윈도우 서명
</details>

## 2026년

### 0614
- TODO: 딥러닝 추론 cancel 기능 및 process bar?(stage별로 무거운 애한테 가중치 적용). ETA 가중치는 yolo ocr 재탐지 bert = 1:5:7:1
    - Progress Bar 추가: SSE 스트림의 stage(yolo, ocr, missing, bert)마다 부여된 가중치를 기준으로 현재 딥러닝 처리 진행도를 퍼센트 및 애니메이션 프로그레스 바 형태로 시각화
    - ETA(남은 시간) 로직 구현: 
        - 단순 비율 연산으로 인해 초기 ETA가 과도하게 산정되는 이슈 해결
        - 동적 베이지안 평균(Dynamic Bayesian average)을 사용하여 첫 번째 이미지의 YOLO 연산 소요 시간 기준으로 ETA 초기 가중치를 재산정하는 로직 구현 (초기 cold start 시간은 0.5 discount 처리)
        - 이전 소요 시간 대비 1.15배 이상 급증하지 않도록 상단 제약선 설정하여 튀는 현상 방지
        - UI적으로 안정된 실시간 Countdown Ticker 기능 개발
    - OCR 연산 강제 취소(Cancel) 기능 및 백엔드 연동:
        - 진행 중 파이프라인(GPU 등 백엔드 작업)을 중지하기 위한 `/api/ocr/transactions/cancel/<job_id>` API를 신설
        - 프론트엔드의 취소 팝업 버튼과 연결하여, 클릭 시 즉시 SSE 연결 종료, 프론트엔드 state 정리, 서버 GPU 파이프라인 강제 예외 처리 및 정리 프로세스로 돌아가도록 구현
        
### 0530
- TODO Resolves
    - TODO: 패키지 버전 재시작 로직 개선(refresh 기반으로 최소 동작 보장 등), dev버전은 그냥 알아서 껐다켜자
        - relaunch시 python 프로세스 종료 후 재시작하도록 구성
    - TODO: 데이터 경로 원복(리셋) 버튼 추가
        - 기본 경로 API 신설 및 초기화 확인 팝업 추가
- MPS동작 확인: YOLO/BERT 디바이스 및 모델 경로 로그 출력 추가
- OCR 모듈 경로 정리: 개발환경 pororo 모듈 경로 계산 수정

### 0528
- repo분리
    - 앱자체만 담은 새 repo로 이동.
- 데이터 경로 분리/이동 로직 정리
    - data_path.db 기반으로 DB 경로 분리
    - 기존 DB 이동 + 덮어쓰기(force) 분기 추가
    - 덮어쓰기 취소 시 새 경로의 기존 DB 사용하도록 분기
    - 이동 후 기존 빈 폴더 정리
    - data_path.db를 이용하여 전체 db파일 참조하도록 변경
- 설정 모달 개선
    - 이동/덮어쓰기 확인 팝업 추가
    - 팝업 클릭 버블링 이슈 수정

### 0527
- 맥 환경 개선
    - os가 맥인경우 Title bar 분기: 상단 타이틀 버튼 이후에 배치, 기존 윈도우용 버튼 감춤
- Help 동작 추가: 버튼 누르면 기본 브라우저로 Help 페이지 열기
- Settings 페이지 신설
    - 설정 버튼 누르면 설정 모달 팝업 표시

### 0526
- 맥북 개발 환경 재정리
    - Node/npm 설치 후 frontend 의존성 재설치
    - Electron 버전 확인 및 dev 실행 흐름 재점검
    - backend Python은 `custommydata-mac` conda 환경으로 고정
    - M1 Air에서 PyTorch MPS 사용 가능 여부 확인
- 실행 포트 정리
    - macOS의 `5000` 포트가 `AirTunes/ControlCenter`와 충돌하는 문제 확인
    - Flask 백엔드는 `5050`으로 이동
    - Vite/Electron dev 서버는 `5173` 기준으로 맞춤
    - Electron dev의 backend 대기 URL과 프론트 API 기준 주소를 새 포트로 통일
- 모델/백엔드 정리
    - 학습 모델 파일을 `customMydataService/models`에서 공통으로 읽도록 정리
    - OCR / 분류 모델 초기화가 macOS에서도 정상 동작하는 것 확인
    - `/api/health` 응답 확인 및 백엔드 기동 안정화
- 프론트-백엔드 연동 정리
    - 카테고리, 거래내역, 대시보드, 매핑 등 프론트 API 주소를 `127.0.0.1:5050`으로 정리
    - `Reset` 버튼이 `/api/initialize-defaults`를 통해 계좌/카테고리를 초기화하도록 연동
    - `category_utils.py`의 하드코딩 기본 카테고리로 다시 채우는 흐름 복구
    - 카테고리 화면의 저장/불러오기 연동과 기존 포트 참조 문제 정리
- 맥용 빌드 정리
    - 패키지명 영문으로 변경하여 맥에서 실행되지 않는 문제 해결



## 2025년
<details>
<summary>2025 Patchnote</summary> 

### 0901
- Electron으로 웹 기반 애플리케이션 개발, Python으로 백엔드 처리, SQLite로 로컬 DB 저장 방식으로 전체 가닥을 잡음
- 기존 가계부 기능 + 딥러닝 서비스 추가하여 웹앱 기반 애플리케이션 개발

### 0904
- 카테고리 메뉴
    - 계좌 및 카테고리 추가/수정/삭제 기능을 구현하였으나 수정 시 거래내역에서 에러가 발생하여, UUID 도입. UUID 관련 코드로 모두 수정 필요
- 거래내역 메뉴
    - 필터 기능 및 드롭다운 입력 기능 구현
    - 열 추가/삭제 기능 구현, 저장 및 초기화 구현
    - 카테고리 메뉴와의 연결이 불안정하여, 카테고리 메뉴 안정화시킨 후 이어서 제작

### 0907
- 전체적인 함수 및 HTML 수정, 팝업 및 추가/수정/삭제 로직 안정화
- 화면자체는 정상적으로 출력되나, DB연계 과정에서 type 불일치 등 에러 발생
- 데이터가 모두 이름만을 기준으로 처리되고 있어, 계좌DB부터 id를 기준으로 처리하도록 수정 중
- 계좌 DB 수정 후 카테고리도 UUID를 기준으로 프런트에서 처리하도록 수정 필요
- 모두 수정된 후 다시 저장이 동작하는지 테스트 필요

### 0908
- ~~카테고리 UUID를 기준으로 처리하도록 수정, 대분류의 경우 첫글자를 따서 majorUuid로 처리~~
- 소분류에서 진짜 uuid 시스템 적용, 대분류와 소분류 테이블 분리, DB 시스템 완전히 재구성
- 백엔드 및 프런트 시스템 완전히 재구성 필요(database.py, account_utils.py까지 완료)
- 계좌 DB 초기화 로직 추가 필요 ( V )
> ???: "젠장, 망했다!"

### 0909
- 카테고리 메뉴 DB 시스템 재구성 완료(대분류 테이블, 소분류 테이블로 분리)
- 새 백엔드 api에 맞게 프런트 데이터 처리과정 수정
- 초기화 및 저장 로직 완성

### 0910
- 바뀐 카테고리 메뉴에 맞춰 거래내역 DB도 재구성
- 재구성된 백엔드 api에 맞춰 프런트 데이터 처리과정 수정
- 상단 툴박스와 행 제목은 고정하고 내용물만 스크롤되도록 개선
- 형광펜 기능 구현중 ( V )

### 0911
- 형광펜 기능+플래그, 배경 색상 기능 구현(관련된 벡엔드 코드 수정)
- 저장 수행 로직 개선(팝업 분기, 취소 시 하이라이트)
- 편집중 외부 클릭시 취소되던 문제 개선
- 팝업에 optional title 추가

### 0913
- Mapping 메뉴 추가, 관련 DB 신설, Sidebar 링크 추가
- 딥러닝 출력-카테고리 매핑 UI추가, 드래그 이동 구현
- 필터 팝업 component 폴더로 이동
- 거래내역 초기화 오류 수정(app.py - database.py간 함수병 일치)

### 0914
- OCR 보정 메뉴 추가
- 전체 CSS 스타일 통합: index.css를 기준으로 색상, 스타일이 공유될 수 있도록 전체 css파일 수정 필요( V )
- TODO: 거래내역 필터에서 다중 정렬 추가 필요( X ) -- 엑셀도 없음 이런건
- TODO: confirm이나 input, alert, destructive에서 esc 누르면 취소로 기능 ( V )

### 0915
- css스타일 통합 완료
- ESC 팝업닫기 구현
- 상호명-카테고리 룰베이스 매핑 구현: OCR보정 메뉴와 유사하나 대분류-소분류 선택을 드롭다운 리스트로 구현
- 코드 요청이 거의 한계에 다다라서, AI모델 젬미니2.5에서 지피티5미니로 변경
- TODO: 룰베이스 매핑 코드 참고해서 거래내역 리스트 팝업 개선 필요 ( V )

### 0916
- 거래내역 리스트 팝업으로 개선, 스타일 생성
- 날짜 선택 중 외부 클릭 시 렌더 버그 수정
- 달력 스타일 테마 통일

### 0918
- 업로드 모달 팝업 생성 및 백엔드로 전송 구현
- 딥러닝 자동입력 관련 백엔드 API 신설
- OCR -> DF 보정 로직 수정(토스UI 대응)
- 업로드 후 반환 값 표시 팝업 UI 제작중(minor_uuid를 기반으로 값을 읽어오지 못하고 있음)

### 0919
- 전체 백엔드 코드를 뜯어본 후 문제가 되는 부분 모두 수정
    - 딥러닝 모델이 id를 반환하도록 개선
    - 노트북 파일에 있던 재탐색 로직 추가
    - 날짜 보정 로직 백엔드에 추가
    - 자동보정/룰베이스 적용 분기 개선
    - 자동보정/룰베이스 PATCH API 추가
    - 기타 관련된 백엔드 코드 수정
- 일단 팝업 및 데이터 삽입, 보정 규칙 모두 동작
- 파일명 클릭 시 이미지 팝업 추가
- 계좌 변경 시 동일한 파일명은 같은 계좌로 적용하며 하이라이트 효과
- 삽입 클릭 시 빈 셀이 있을 경우 하이라이트 효과를 주며 경고 메시지 출력
- 더치페이 기능 구현중
    - 기능 열 생성 및 UI렌더 방식 재구성, 취소 기능 구현 및 정산금액 계산 가능
    - 데이터 처리 기능 구현 및 메모 생성

### 0923
- 더치페이 기능 완성
    - 행 추가 및 삭제, 자잘한 버그들 수정(border color, 경고 렌더, 계좌 동기화 등)
- 거래내역 폼 생성
    - 기본 UI 생성 및 달력 팝업, 날짜 선택 selectPopup 구현
    - 리스트 선택란 생성 및 유형 선택시 적절한 초기화 로직 구현
    - 금액 필드 서식 적용
    - 입력 시 거래내역 메뉴와 연계하여 새 거래내역 생성
- 그 외 변경사항
    - FloatingSelectPopup에서 선택되어 있는 항목으로 자동 스크롤 및 강조 표시
    - DatePickerOverrides.css에서 요일 행 가운데 정렬 서식 지정 및 한글화
- 수정필요사항
    - 딥러닝 자동입력에서 다른ui에서 날짜 생성 시 날짜형식이 통일되지 않음( V )

### 0924
- 거래내역 폼 고도화
    - 검색 기능 및 내계좌이체 기능 추가
    - 확장 패널 UI구성 및 각 기능 연결
    - 입력 시 거래내역 메뉴에서 스크롤 및 하이라이트 효과 구현, 종료 시 입력한 행의 수 계산
    - TODO: 달력 팝업 열 때 나머지 요소 밀리는 현상 수정 필요 ( V )
- 그 외 변경사항
    - 거래내역 메뉴 초기 진입시 맨 아래로 자동 스크롤
    - DatePickerOverrides.css 에서 div요소가 gap을 만드는 요소 해결(0924TODO)
    - ocr_service.py에서 통일된 날짜를 반환하도록 개선 (0923TODO)

### 0926
- 월력형 메뉴 신설
    - 날짜 선택, 기본 UI, 백엔드 데이터 불러오는 로직 생성
- TODO: 거래내역 메뉴에서 0원이 입력된 경우 NULL로 롤백 ( D )

### 0929
- 월력형 메뉴 고도화
    - 셀 높이 고정 및 스크롤하도록 변경
    - 날짜 클릭시 상세 거래내역 신설
- 그외 월력형 메뉴 포함 다른 UI 스타일 세부 조정
- 거래내역 메뉴 기능 신설: 엑셀 툴 추가
    - 엑셀 템플릿 받기: 거래내역 메뉴와 동일한 행 제목을 받는 엑셀 템플릿 다운로드
    - 불러오기: 템플릿에 거래내역을 입력하여 불러오는 기능 생성
        - 새 UI생성: 프리뷰팝업 모달과 유사하며, 삽입 시 빈 셀 감지 및 편집 기능 제공
        - 엑셀의 특이한 날짜 형식을 여기 시스템과 일치하도록 변환
        - 텍스트를 기준으로 일치하는 항목을 찾아 연결해주며, 찾지 못한 경우 placeholder로 기존 값을 보여주어 원래 값을 참고하여 새 값을 선택할 수 있도록 도움
    - 내보내기: 전체 거래내역을 엑셀로 내보냄
- 대시보드 메뉴 신설(갈길이 멀다....)

### 0930
- 대시보드 관련 백엔드 API 신설
- 대시보드 UI구성(월 선택, 기간 선택 슬라이더 등) 신설
- 기간별 차트 출력 카드 제작중
- 슬라이더가 예쁘게 나오지 않는 이슈에서 벗어나지 못하고 있음.....

### 1001
- 대시보드 UI조정
    - 슬라이더 및 셀렉터를 헤더 영역으로 이동
    - 슬라이더 UI 재구성 및 색상 팔레트 조정
- 차트 카드 조정
    - 수입/지출별로 묶은 차트에서 signed 묶은 차트로 변경
    - 수입/지출차를 비교하기 어려워졌으므로 차액을 계산하여 묶은 차트에 포함하여 흐리게 표시
    - 부족/초과량을 커스텀 툴팁과 범례로 '차액'으로 묶어서 표시
    - 차트 스타일 및 툴팁 스타일 전체적인 시스템과 어울리도록 조정
    - x축 간격(div) 따라서 x축 label의 건너뛰기 개수 조정
    - 각 총 수입 및 지출 label출력(이거만 몇시간을판거야미친거아니야)

### 1002
- 월간 상세 분석 카드 신설
    - 좌측에 수입별 차트 표시, 우측에 대분류별 지출 표시

### 1003
- 월간 상세 분석 차트 레이아웃 조절
    - 커스텀 범례 사용하여 차트 하단 적절한 위치에 표시되도록 조정
    - 커스텀라벨로 각 유형별 비율 표시
    - 대분류별 지출, 계좌 잔고 표 표시

- 대분류별 지출 비율 treemap 추가
    - 색상 테마 기존 스타일과 유지하며 지출 비율을 알 수 있도록 treemap으로 렌더
    - 툴팁으로 각 요소의 금액 확인가능(tooltip unload가 안되어서 시간 왕창뺐음)
    - 각 소분류 요소에도 라벨이 추가되도록 조정
- 그 외 수정사항
    - 월간 추이의 툴팁 스타일 월간 상세 분석과 통일
    - 카드별 크기 500px -> 512px로 조정 (표 리스트 개수 고려)
    - 스크롤바 핸들이 요소 끝에 다다른 경우 자동으로 스크롤이 따라붙도록 UX개선

### 1004
- 기간 내 소비 TOP10 카드 신설
    - N개월 합산<->평균 버튼 생성 및 차트 Label 변경 기능 추가
    - TOP10 API 생성 및 연동, 차트로 표시(좌우로 뻗는 형태)
    - 배경 tickCount를 custom으로 만드는데 오래걸림(기본 tickcount는 원하는대로 안먹어서, CartesianGrid를 가지고 커스텀으로 width에 맞춰서 표시하도록 제작)
    - Tooltip에 순위 및 금액 표시
    - **디테일한 부분이 아쉬움...좀 더 디벨롭 필요
- 그 외 수정사항
    - 코파일럿 지침 파일 copilot-instruction.md 생성. 이제 매번 새로 지침 교육을 하지 않아도 얼추 알아들음
    - 대분류별 지출 비율 타입 에러 해결: 전달하는 타입 as any 키워드로 에러 우회
    - 프런트 빌드/백엔드 서버 실행 자동화 배치파일 생성

### 1005
- 기간 내 소비 TOP10 수정사항:
    - 툴팁 강화: 툴팁에 거래내역 디테일 강화(거래처 및 금액 표시) 및 관련 API 신설
    - 동일 라벨 강조: 양쪽 차트에 같은 라벨이 나오는 경우 highlight 효과 적용

### 1006
- 예산 관리 카드 신설
    - 예산 정보 저장 테이블 생성 + CRUD 함수 생성 및 호출 API 생성
    - 지출 집계는 Treemap API 재사용(프런트에서 데이터만 가공)
    - 표 레이아웃 구성 및 월간 상세 분석에서 사용한 progressBar 레이아웃 재사용
    - 예산 세팅 수정 시 바로 백엔드에 반영하도록 구성

### 1007
- 예산 관리 카드 개선
    - 그래프 컬럼별 색상이 아닌 구간별 색상으로 변경
    - 수정/삭제 overlay 스타일 카테고리 메뉴처럼 변경 및 input 시 UI가 깨지는 문제 수정
    - 다른 컬럼에서 동일한 대분류가 선택되지 않도록 안정장치 마련 및 이중으로 관리되던 상태 통합
    - FloatingSelectPopup에 unrecommeded state추가하여 카테고리 금액이 0인 항목 어두운 색으로 전달
- 고정비 관리 카드 신설
    - '고정지출'유형 중 2개월 이상 지속된 항목만 집계하여 표시(관련 API 추가)
    - 툴팁으로 전월대비 증감 및 결제 여부 표시
    - 팝업으로 세부 거래내역 표시
        - 스크롤시 닫히는 로직이 제대로 잡히질 않아서 상위 컴포넌트와 연계하도록 제작
        - 팝업 on/off 시 Top10 차트가 리렌더링 되면서 LabelList가 깜빡이는 문제 수정
- TODO 해결
    - TODO: 예산 관리 메뉴에서 Treemap을 불러올때 0원인건 list에서 흐리게 표시( V )
        - SelectListPopup쪽에 unrecommended 옵션을 추가하여 어두운 색으로 렌더하도록 구현
    - TODO: 대시보드에서 기간 말고 년/월도 선택 값 저장 필요 ( V )
        - 관련 백엔드 API 신설 및 대시보드 페이지 수정

### 1008
- 자산 포트폴리오 카드 신설
    - 파이차트 / 영역차트 탭으로 구성
    - 파이차트 및 표로 구성된 자산 비율 및 금액 표시(근데 너무 애매함....없앨까)
    - 누적 영역 차트로 자산 변화 표시 및 툴팁으로 매 달 디테일 표시
    - Brush 활용하여 그래프 확대 및 스크롤 지원
- TODO 해결:
    - TODO: https://recharts.org/en-US/examples/BarChartWithMultiXAxis 를 보면 X축을 두개로 만들 수 있던데 월별 추이에 적용 가능한지 검토 ( V ) : 새 계산 방식 적용하여 이중 X축 스타일로 개선

### 1009
- 파이 차트 '월별 상세' 카드의 '계좌 잔고' 옆 버튼 뚫어서 팝업으로 이동
- 자산 포트폴리오 카드는 누적 영역 차트만 표시, X축 tick 형식 수정
- 고정지출 메뉴 변경:
    - 스크롤 시 닫기 방식 스크립트 내부에서만 처리하도록 수정
    - hover시 첫번째 두번째 열만 배경색이 바뀌며, 텍스트는 동시에 바뀌도록 수정
    - 고정비 합산 로직 변경 및 월별 지출 현황이 제대로 게산되지 않던 문제 수정

### 1010
- 자산 포트폴리오 메뉴 변경:
    - 툴팁에서 비율을 표시하며 정렬된 레이아웃을 갖도록 개선
    - 툴팁과 범례의 순서가 일치하도록 커스텀 범례 신설
    - 전체적인 레이아웃 개선
- 월별 예산 관리 메뉴 변경: 다음 색상 구간 5% 전부터 그라데이션으로 표시하도록 변경
- 소비 패턴 인사이트 신설:
    - 관련 백엔드 API 추가. 10가지 타입 인사이트를 제공하는 쿼리 생성
    - 좌우 레이아웃으로 요일별 지출 금액 및 인사이트 목록 출력
    - hover시 툴팁으로 이어지는 효과, 인사이트 hover시 해당 셀 강조 효과 적용
    - 설정 버튼 클릭 시 팝업으로 임계값 세팅 팝업 출력

### 1011
- 소비 패턴 인사이트 개선:
    - 설정 팝업 추가: 인라인 input 스타일로 설정 조정
    - 백엔드와 연계하여 인사이트 threshold를 직접 조절할 수 있도록 변경
    - 초기화 버튼 confirmPopup을 사용하도록 적용하였으며, z-index 순번을 조절하기 위해 confirmPopup의 z-index 조절

### 1012
- 거래내역 메뉴 개선:
    - 지출 유형끼리 이동할 때 및 완전히 동일한 유형끼리 움직일 때 대분류가 초기화되지 않도록 개선
    - 대분류 미 선택 시 소분류 선택 비활성화 및 알림 출력
    - 달력 팝업에서 월/년을 select를 통해 버튼으로 이동이 아닌 직접 이동할 수 있도록 개선(개오래걸림...), 특히 필터 팝업은 거래내역의 데이터에서 최대/최소를 추출하여 전달(각종 폼 및 필터 등 하위 요소까지 모두 적용)
- 월별 예산 메뉴 개선: 범례 추가
- 소비 패턴 인사이트 기본값 조정
- TODO 해결:
    - TODO: 대분류별 지출 비율에서 만든 로딩스타일 딥러닝 대기에 추가 ( V ) 
    - TODO(remind): 거래내역 메뉴에서 0원이 입력된 경우 NULL로 롤백 ( V )
    - TODO: 필터 기능 강화: 날짜는 기간으로도 설정이 가능하게, 필터링 시 현재 화면이 있는 것들로만 체크가 가능하도록 리스트 재구성( V )

### 1013
- Electron을 사용하여 패키지로 빌드
    - 패키지 빌딩 관련 설정 추가
    - 당장은 백엔드와 프런트 분리가 어려워, 로컬에서 서버를 실행시키며 프런트 요소로 확인하는 방식으로 동작
    - 프로그램 전용 가상환경 .venv_backend 생성, 뽀로로 모듈 및 해당 가상환경 전체 복사하는 방식으로 패키징
    - Titlebar 생성, 이에 따라 app.tsx의 레이아웃 일부 조절
    - 빌드하기 위해 전체적으로 사용되지 않는 변수 및 함수 전체 정리
    - 빌드용 DB 경로 및 기타 백엔드 에셋 경로 지정 함수 신설


### 1215
- TODO: 파이썬 호출시 패키지에 있는 파이썬이 아닌 appdata/local에서 찾아서 다른 유저일때 실행되지 않는 문제(V)
    - 파이썬 임베디드 버전을 받아서 해결, 가상머신에서 동작 확인

</details>






